apiVersion: v1
kind: Service
metadata:
  name: redis-cache
  namespace: vehicles-prod
  labels:
    app: redis-cache
spec:
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
  selector:
    app: redis-cache
