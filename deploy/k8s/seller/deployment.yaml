apiVersion: v1
kind: Service
metadata:
  name: seller-service
  namespace: vehicles-prod
  labels:
    app: seller-service
spec:
  type: ClusterIP
  selector:
    app: seller-service
  ports:
    - name: grpc
      port: 50057
      targetPort: 50057
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: seller-service
  namespace: vehicles-prod
  labels:
    app: seller-service
spec:
  replicas: 1
  revisionHistoryLimit: 10
  progressDeadlineSeconds: 300
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: seller-service
  template:
    metadata:
      labels:
        app: seller-service
    spec:
      containers:
        - name: seller-service
          image: europe-west1-docker.pkg.dev/PROJECT_ID/vehicles/seller:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 50057
              name: grpc
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: seller-env
                  key: SELLER_DATABASE_URL
            - name: LISTING_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: seller-env
                  key: LISTING_DATABASE_URL
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          startupProbe:
            grpc:
              port: 50057
            failureThreshold: 30
            periodSeconds: 5
          livenessProbe:
            grpc:
              port: 50057
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            grpc:
              port: 50057
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
