apiVersion: v1
kind: Service
metadata:
  name: auction-service
  labels:
    app: auction-service
spec:
  selector:
    app: auction-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
    - name: ws
      port: 8081
      targetPort: ws