apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: vehicles-prod
  labels:
    app: user-service
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - name: grpc
      port: 50058
      targetPort: grpc