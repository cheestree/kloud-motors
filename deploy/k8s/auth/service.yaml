apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: vehicles-prod
  labels:
    app: auth-service
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - name: grpc
      port: 50050
      targetPort: 50050