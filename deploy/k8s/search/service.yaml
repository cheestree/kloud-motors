apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: vehicles-prod
  labels:
    app: search-service
spec:
  type: ClusterIP
  selector:
    app: search-service
  ports:
    - name: grpc
      port: 50056
      targetPort: grpc