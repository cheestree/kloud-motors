apiVersion: v1
kind: Service
metadata:
  name: chat-service
  labels:
    app: chat-service
spec:
  selector:
    app: chat-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50052
      targetPort: grpc
    - name: ws
      port: 8080
      targetPort: ws
